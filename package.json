{
    "name": "skill-pack-buddy",
    "version": "1.0.0",
    "description": "Pack Buddy skill",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "alexa-test": "cd test && sh run.sh"
    },
    "main": "index.js",
    "dependencies": {
        "alexa-sdk": "^1.0.0"
    },
    "devDependencies": {
        "alexa-app-server": "^3.0.1",
        "eslint": "^4.19.1",
        "eslint-config-google": "^0.9.1",
        "lambda-local": "^1.4.6"
    }
}