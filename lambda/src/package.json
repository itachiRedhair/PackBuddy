{
  "name": "skill-pack-buddy",
  "version": "1.0.0",
  "description": "Pack Buddy skill",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "alexa-sdk": "^1.0.0",
    "aws-sdk": "^2.219.1"
  }
}
